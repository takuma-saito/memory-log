sudo の設定
============================

sudo でパスが引き継がれないため、
上手くコマンドが起動しない時がある.

その時は visudo で以下のように設定して環境がリセットされないように設定する

    $ visudo
    Defaults:username !env_reset

username には環境を引き継がせたいユーザー名を入れればよい.
